import Track from './Track';
import { render, screen } from '@testing-library/react';
import { describe } from 'yargs';
import { expect, test } from '@jest/globals';

describe('Likes Monitor', () => {
    beforeEach(() => render(<Track/>));

    test('', () => {

        expect().toBe();
    })
})